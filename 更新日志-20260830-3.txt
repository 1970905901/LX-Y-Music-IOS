LX-Y Music iOS 更新日志（2026-08-30 · 资源缓存批）

一、资源缓存管理
1. 修复「选了缓存大小上限，实际却超出」：此前上限只清理 WebDAV 云盘子目录，
   而界面显示的是全部应用缓存（含封面缓存、临时文件等），加起来仍会超
2. 新增原生 LRU 清理：按最后修改时间最旧优先删除，将整个应用缓存
   （Caches + Tmp，排除 TrackPlayer 原生目录）收敛到所选上限内
3. 三处自动执行：选择上限时立即清理、WebDAV 下载完成后清理、
   每次加载新歌后清理，日常使用中总大小持续受控
